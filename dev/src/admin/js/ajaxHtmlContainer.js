import 'jquery';
const $ = jQuery

const AjaxHtmlContainer = (options = {})=>{

    const defaults = {
        'containterClass' : 'wpe-ajax-html-container',
        'bodyClass' : 'wpe-ajax-html-body'
    }

    const settings = {...defaults, ...options}


}