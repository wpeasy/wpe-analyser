import 'jquery';
const $ = jQuery

const reports = (options = {}) =>
{
    const defaults = {
        'containerClass' : 'wpe_ajax_container'
    }

    const settings = {...defaults, ...options}
}

export default reports

